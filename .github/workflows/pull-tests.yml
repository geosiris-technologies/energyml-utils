name: Maven Package Energyml-utils

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 11
        uses: actions/setup-java@v5
        with:
          java-version: "11"
          distribution: "temurin"
          cache: "maven"
          server-id: central
          server-username: CI_DEPLOY_USERNAME
          server-password: CI_DEPLOY_PASSWORD
          gpg-private-key: ${{ secrets.OSSRH_GPG_PRIVATE_KEY }}
          gpg-passphrase: MAVEN_GPG_PASSPHRASE

      - name: Compute Maven version
        id: version
        run: |
          SHA=$(git rev-parse --short HEAD)

          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            # Tag -> release version
            REVISION="${GITHUB_REF_NAME#v}"
            CHANGE_LIST=""
            SHA=""
          elif [[ "${GITHUB_REF_NAME}" == "main" ]]; then
            # Main -> snapshot version
            REVISION="${GITHUB_REF_NAME#v}"
            CHANGE_LIST="-SNAPSHOT"
            SHA=".dev${GITHUB_RUN_NUMBER}"
          else
            # Feature or other branch
            REVISION="${GITHUB_REF_NAME#v}"
            CHANGE_LIST=""
          fi

          echo "revision=$REVISION" >> $GITHUB_OUTPUT
          echo "sha1=+$SHA" >> $GITHUB_OUTPUT
          echo "changelist=$CHANGE_LIST" >> $GITHUB_OUTPUT

          echo "Resolved version: ${REVISION}+${SHA}${CHANGE_LIST}"
          # echo "Resolved version: ${REVISION}${CHANGE_LIST}"

      - name: Unit test
        run: |
          mvn -B --file pom.xml test -P test \
            "-Drevision=${{ steps.version.outputs.revision }}" \
            "-Dsha1=${{ steps.version.outputs.sha1 }}" \
            "-Dchangelist=${{ steps.version.outputs.changelist }}"

      - name: Build with Maven
        run: |
          mvn --batch-mode deploy -P release -Dchangelist= "-Drevision=${{ steps.version.outputs.version }}" -Dgpg.keyname="${{ secrets.OSSRH_GPG_KEY_ID }}" \
          "-Drevision=${{ steps.version.outputs.revision }}" \
          "-Dsha1=${{ steps.version.outputs.sha1 }}" \
          "-Dchangelist=${{ steps.version.outputs.changelist }}"
        env:
          GITHUB_TOKEN: ${{ github.token }}
          CI_DEPLOY_USERNAME: ${{ secrets.CENTRAL_USERNAME }}
          CI_DEPLOY_PASSWORD: ${{ secrets.CENTRAL_PASSWORD }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.OSSRH_GPG_PASSPHRASE }}
